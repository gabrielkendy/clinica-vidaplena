node_modules/
*.log
.DS_Store
Thumbs.db

# Material de produção do vídeo (roteiro do canal) — não faz parte do lab público
01-ROTEIRO/

# NOTA IMPORTANTE:
# .env e public/.env NÃO são ignorados DE PROPÓSITO.
# Este lab existe pra demonstrar a falha clássica de arquivo de segredos exposto.
# Todos os valores são FICTÍCIOS (marcados como EXEMPLO). Nunca use dados reais aqui.
